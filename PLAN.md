# gamelife-skill 实施计划

## 当前版本

`0.0.2`。本版本的范围已冻结为三个核心闭环：

1. 所有任务展示和交互面板。
2. 新建任务的 Agent 拆解。
3. 每天四条当天任务推荐。

项目目录位于 `D:\gamelife-skill`，不是 `D:\gamelife_doc\gamelife-skill`。

## 迁移来源

### `D:\gamelife`

这个项目提供了早期微信小程序版的冒险手册任务面板，已确认的界面语言包括：

- 深蓝到青绿色渐变 Hero 区。
- “冒险手册 / 正在发生的冒险”层级。
- 主线/支线章节筛选。
- 全部/进行中/已完成筛选。
- 搜索任务或描述。
- 任务卡片、完成勾选、进度条、章节标签和编辑/删除操作。
- 新建/编辑任务表单：名称、描述、章节、类型、优先级、截止日期和进度。

### `D:\yuanlife`

这个项目提供了更完整的任务域和 AI 业务规则，作为行为和数据模型来源：

- `chapters`、`tasks`、`subtasks`、`daily_commissions` 等对象关系。
- 任务类型：学习、工作、健康、创作、生活、复盘和其他。
- 任务状态：待开始、进行中、已完成、取消、归档。
- 优先级：低、普通、高、紧急。
- 子任务按顺序和预计分钟数组织。
- 复杂任务拆成 2–5 个轻量、可执行子任务的原则。
- 每日委托结合优先级、截止时间、准备条件和阻塞关系生成。

数据库、登录、奖励流水、地图探索、等级系统和后端 API 不迁移到 `0.0.2`，除非任务面板运行确实需要兼容字段。

## 0.0.2 的产品行为

### 任务面板

HTML 必须是可操作界面，而不是截图。用户可以查看全部任务、筛选、搜索、编辑、完成、删除和查看子任务；面板同时显示整体完成率和今日四项推荐。

### 新建任务拆解

Agent 收到用户输入后，先输出：

- 用户原话对应的任务标题和描述。
- 是否需要拆解以及判断理由。
- 子任务顺序、依赖和预计耗时（有依据时）。
- 缺失信息和需要用户确认的字段。

用户确认前只保存为草稿/建议，确认后才写入状态。HTML 内置一个无网络的本地预览兜底，但它不替代 Agent 的语义分析。

### 每日四项行动

每个自然日使用日期作为幂等键，首次打开工作台或调用 Skill 时生成一次。推荐优先引用已有未完成任务，综合重要性、紧急程度、截止日期、准备条件、解阻塞价值和当天负荷。

界面保留四个推荐位。可选任务少于四个时显示“待补充”或明确标注“建议新建、等待确认”，不能伪造已有任务。

## 对话来源边界

其他对话指 Codex 和 ChatGPT 的其他对话。`0.0.2` 不接受导出或上传其他对话，也不把手动复制对话作为正式流程；只有运行环境实际把指定对话提供给 Agent 时才可读取。历史对话连接器作为 `0.2` 或更后续版本的可插拔能力。

## HTML 双端策略

同一份 HTML 使用响应式布局：

1. 电脑端是 `0.0.2` 的第一验收端。
2. 手机浏览器同步验证，优先保证核心操作和可读性。
3. 如果双端装饰细节冲突，优先任务操作、输入可用性和移动端卡片可读性。

## 版本策略

- `0.0.x` 只能做 bug、兼容性、样式和文案修复。
- 改变状态字段时必须提升 `schema_version` 并提供迁移说明。
- 增加新产品模块或历史对话连接器时进入 `0.2` 评估。
- 每个发布节点更新插件清单、`CHANGELOG.md`、README、测试和 Git tag。

## 当前实现顺序

1. Skill 入口与行为规范。
2. 数据契约与校验/推荐脚本。
3. HTML 任务面板和本地持久化。
4. 任务拆解预览与子任务交互。
5. 四项每日推荐及幂等逻辑。
6. 本地测试、Skill 静态校验、浏览器验证和 GitHub 发布。
